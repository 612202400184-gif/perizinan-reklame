<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Admin - Verifikasi Berkas</title>
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
</head>
<body>
    <div class="container">
        <h2>Panel Admin: Verifikasi Dokumen</h2>
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Dokumen</th>
                    <th>Status</th>
                    <th>Aksi</th>
                </tr>
            </thead>
            <tbody>
                @foreach($permohonan as $p)
                <tr>
                    <td>{{ $p->id_permohonan }}</td>
                    <td>
                        @foreach($p->dokumens as $doc)
                            <a href="{{ asset('storage/'.$doc->filePath) }}" target="_blank">[{{ $doc->tipe_dokumen }}]</a>
                        @endforeach
                    </td>
                    <td>{{ $p->status }}</td>
                    <td>
                        @if($p->status == 'Pending')
                        <a href="{{ route('admin.verifikasi', $p->id_permohonan) }}" class="btn-primary" style="background: #059669;">Verifikasi</a>
                        @endif
                    </td>
                </tr>
                @endforeach
            </tbody>
        </table>
    </div>
</body>
</html>
